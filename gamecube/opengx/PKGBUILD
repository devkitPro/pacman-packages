pkgname=gamecube-opengx
pkgver=0.9.0
pkgrel=1
pkgdesc='opengl/gx translation library'
arch=('any')
url='https://github.com/devkitPro/opengx'
license=('BSD')
options=(!strip libtool staticlibs)
makedepends=('catnip')
depends=('libogc')
groups=('gamecube-portlibs')

source=(
  "opengx-${pkgver}.tar.gz::${url}/archive/refs/tags/v${pkgver}.tar.gz"
)

build() {
  cd opengx-${pkgver}
  catnip -t gamecube
}

package() {
  cd opengx-${pkgver}
  DESTDIR=${pkgdir} catnip install
}

sha256sums=('4cbf16ec3232c64bd62440fe58b287fdf062e48780f8a1d839a9d4718c5ed05c')
