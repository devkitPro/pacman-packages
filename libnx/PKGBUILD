
# Maintainer: Dave Murphy <davem@devkitpro.org>

pkgname=('libnx')
pkgver=4.1.0
pkgrel=1
pkgdesc="Nintendo Switch AArch64-only userland library."
arch=('any')
license=('custom')
url="http://github.com/switchbrew"
options=(!strip libtool staticlibs)
source=(
  ${pkgname}-${pkgver}.tar.gz::${url}/${pkgname}/archive/v${pkgver}.tar.gz
)
sha256sums=(
  '76d1bd946cf4db7d4e6e316da1e383bc8a05d93d19452aa32c0432dacb41eddc'
)
makedepends=('devkitA64')

groups=('switch-dev')

build() {

  cd ${srcdir}/${pkgname}-${pkgver}/nx
  make

}

package() {

  cd ${srcdir}/${pkgname}-${pkgver}/nx
  make DESTDIR=$pkgdir install

}
